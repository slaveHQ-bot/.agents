# Agent Initialization Prompt

Your assignment is located in `tasks/active/TASK-[ID].md`.

Read the task file entirely. Load every required context file listed in Section 2, plus applicable `.ai/` and local `AGENT.md` rules.

Before changing anything, analyze the existing codebase: locate the current implementation, interfaces, callers, consumers, tests, persistence, configuration, and all UI/IPC/RPC/API connections affected by the task. Reuse or modify existing canonical functionality instead of creating duplicates.

Execute the task strictly step-by-step. Run the verification command after every step. Do not advance while verification is failing. Diagnose and repair failures at the root cause; retry up to 2 times. If still unresolved, stop and ask for clarification.

Before declaring completion, test all affected connections end-to-end where practical, run the required build, update the task ledger with real evidence, and synchronize the Solution Book and applicable `AGENT.md` files.

Never claim a test/build passed unless it was actually executed.
